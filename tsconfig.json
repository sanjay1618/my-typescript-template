{
  "compilerOptions": {
    /* Language and Environment */
    "target": "ES6",                         // JavaScript version output (modern browsers/Node.js)
    "module": "CommonJS",                     // How modules are handled (Node.js standard)
    "lib": ["ES6", "DOM"],                    // Include standard libraries (browser and JS features)

    /* Project Structure */
    "rootDir": "./src",                       // Where TypeScript files are located
    "outDir": "./dist",                       // Where compiled JavaScript should go
    "strict": true,                           // Enable all strict type-checking options

    /* Helpful Flags */
    "esModuleInterop": true,                  // Allow default imports from CommonJS modules
    "forceConsistentCasingInFileNames": true,  // File names must match casing (important on Linux)
    "skipLibCheck": true,                      // Skip checking type files from node_modules
    "allowSyntheticDefaultImports": true,      // Makes default imports easier

    /* Source Maps for Debugging */
    "sourceMap": true                         // Allows debugging TypeScript directly in the browser or VS Code
  },
  "include": ["src"],                         // Only compile files in 'src'
  "exclude": ["node_modules", "dist"]          // Don't compile dependencies or output folder
}
